{
  "name": "pass-scout-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:static": "NEXT_PUBLIC_STATIC_MODE=1 next dev",
    "dev:clean": "rm -rf .next node_modules/.cache && next dev",
    "build": "next build",
    "build:static": "NEXT_PUBLIC_STATIC_MODE=1 next build",
    "build:clean": "rm -rf .next node_modules/.cache && next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.7.0"
  }
}
